RIETA Safe Installer

Use this instead of install_rieta_full_chatbot.php if the old installer returns HTTP 500.

Upload:
install_rieta_safe.php

Open:
https://investoraccess.unidatechs.com/install_rieta_safe.php

Delete after success:
install_rieta_safe.php

This installer embeds files using base64, so it avoids the earlier 500 caused by broken embedded PHP/JSON strings.
